services:
  academy:
    build:
      context: ../../KCS Nexus Academy
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env.staging
    expose:
      - "3000"
    networks:
      - academy_staging_front
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
networks:
  academy_staging_front:
    name: kcs_academy_staging_front
